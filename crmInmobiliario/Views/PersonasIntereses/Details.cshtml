@model crmInmobiliario.Models.PersonasIntereses

@{
    ViewBag.Title = "Details";
}

<h2>Datos sobre Interes sobre la propiedad</h2>

<div>
    
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Interes)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Interes)
        </dd>

    </dl>
</div>
<div class="row">
    <div class="col-md-12">
        <p>
            <button class="btn btn-primary" onclick="location.href='@Url.Action("Index", "PersonasIntereses")';return false;"><i class="fa fa-chevron-left"></i> Volver</button>
            <button class="btn btn-success" onclick="location.href='@Url.Action("Edit", "PersonasIntereses", new { id = Model.IdInteres })';return false;"><i class="fa fa-pencil"></i> Editar</button>
        </p>
    </div>
</div>