@model crmInmobiliario.Models.PersonasValidacion

@{
    ViewBag.Title = "Delete";
}

<h2>Eliminar Validación de Documento Presentado</h2>


<div class="alert alert-danger" role="alert">
    <strong>ATENCIÓN !!!</strong> <h3>Esta seguro(a) de Eliminar esta Validación de Documento Presentado?</h3>
</div>
<div>
    
    <hr />
    <dl class="dl-horizontal">
      

        <dt>
            @Html.DisplayNameFor(model => model.DocumentosCategoria.Categoria)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.DocumentosCategoria.Categoria)
        </dd>

        

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

    <div class="form-actions no-color">
        <button class="btn btn-primary" onclick="location.href='@Url.Action("DetailsProspectoValidar", "Personas", new { id = Model.Persona })';return false;"><i class="fa fa-chevron-left"></i> Volver</button>
        <button class="btn btn-danger" type="submit"><i class="fa fa-trash"></i> Eliminar</button>
    </div>
    }
</div>
