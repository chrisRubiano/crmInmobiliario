@model IEnumerable<crmInmobiliario.Models.EdificiosDetalle>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nivel)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.M2)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Edificios.Edificio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nivel)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.M2)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Edificios.Edificio)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdEdificioDetalle }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdEdificioDetalle }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdEdificioDetalle })
        </td>
    </tr>
}

</table>
