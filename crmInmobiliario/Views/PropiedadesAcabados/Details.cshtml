@model crmInmobiliario.Models.PropiedadesAcabados

@{
    ViewBag.Title = "Details";
}

<h2>Datos del Acabado</h2>

<div>
    
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Acabado)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Acabado)
        </dd>

    </dl>
</div>
<div class="row">
    <div class="col-md-12">
        <p>
            <button class="btn btn-primary" onclick="location.href='@Url.Action("Index", "PropiedadesAcabados")';return false;"><i class="fa fa-chevron-left"></i> Volver</button>
            <button class="btn btn-success" onclick="location.href='@Url.Action("Edit", "PropiedadesAcabados", new { id = Model.IdAcabado })';return false;"><i class="fa fa-pencil"></i> Editar</button>
        </p>
    </div>
</div>
