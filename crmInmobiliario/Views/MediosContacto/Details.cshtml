@model crmInmobiliario.Models.MediosContacto

@{
    ViewBag.Title = "Details";
}

<h2>Datos de Medio de Contacto</h2>

<div>

    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.MedioContacto)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.MedioContacto)
        </dd>

    </dl>
</div>
<div class="row">
    <div class="col-md-12">
        <p>
            <button class="btn btn-primary" onclick="location.href='@Url.Action("Index", "MediosContacto")';return false;"><i class="fa fa-chevron-left"></i> Volver</button>
            <button class="btn btn-success" onclick="location.href='@Url.Action("Edit", "MediosContacto", new { id = Model.IdMedioContacto })';return false;"><i class="fa fa-pencil"></i> Editar</button>
        </p>
    </div>
</div>
